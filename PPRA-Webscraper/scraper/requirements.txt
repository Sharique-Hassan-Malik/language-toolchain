selenium>=4.6          # 4.6+ ships Selenium Manager, which resolves chromedriver
pandas
openpyxl
google-api-python-client
google-auth
google-auth-oauthlib

# Optional: only used as a fallback if Selenium Manager cannot fetch a driver.
# webdriver-manager

# Dev-only (for `pytest`):
# pytest
